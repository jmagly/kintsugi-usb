# Kintsugi USB — pre-imaging secret-pattern scanner input
# One regex per line; used by scripts/prep-master.sh with `grep -rE`.
# Derived from docs/sanitization-checklist.md Rule 1/2.
# Lines starting with '#' are comments; blank lines ignored.

# Private-key blocks (any tool, any format)
-----BEGIN OPENSSH PRIVATE KEY-----
-----BEGIN RSA PRIVATE KEY-----
-----BEGIN DSA PRIVATE KEY-----
-----BEGIN EC PRIVATE KEY-----
-----BEGIN PRIVATE KEY-----
-----BEGIN PGP PRIVATE KEY BLOCK-----

# API key env-var assignments (common shell forms; match with or without
# 'export ' prefix, so no '^' anchor)
(^|[^A-Z_])ANTHROPIC_API_KEY=
(^|[^A-Z_])OPENAI_API_KEY=
(^|[^A-Z_])AIDER_API_KEY=
(^|[^A-Z_])AWS_SECRET_ACCESS_KEY=
(^|[^A-Z_])AWS_SESSION_TOKEN=
(^|[^A-Z_])GH_TOKEN=
(^|[^A-Z_])GITEA_TOKEN=
(^|[^A-Z_])HUGGING_FACE_HUB_TOKEN=
(^|[^A-Z_])HF_TOKEN=

# Credential-like literal values (heuristic)
sk-[a-zA-Z0-9]{20,}
xoxb-[0-9]{10,}
ghp_[A-Za-z0-9]{30,}
gho_[A-Za-z0-9]{30,}
ghu_[A-Za-z0-9]{30,}
ghs_[A-Za-z0-9]{30,}
ghr_[A-Za-z0-9]{30,}
